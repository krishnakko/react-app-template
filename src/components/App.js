import React, { Component } from "react";

//Creating a react component

class App extends Component {
  render() {
    return (
      <div>
        <h1>React App</h1>
      </div>
    );
  }
}

export default App; // allows the other files to import this Component
